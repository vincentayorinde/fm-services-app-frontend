import { AdminRequestSearchPipe } from './admin-request-search.pipe';

describe('AdminRequestSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminRequestSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
