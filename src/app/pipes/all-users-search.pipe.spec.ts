import { AllUsersSearchPipe } from './all-users-search.pipe';

describe('AllUsersSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AllUsersSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
