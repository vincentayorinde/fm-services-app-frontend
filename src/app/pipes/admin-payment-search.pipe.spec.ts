import { AdminPaymentSearchPipe } from './admin-payment-search.pipe';

describe('AdminPaymentSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminPaymentSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
