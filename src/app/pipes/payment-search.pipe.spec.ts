import { PaymentSearchPipe } from './payment-search.pipe';

describe('PaymentSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
