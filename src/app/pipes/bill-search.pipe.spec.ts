import { BillSearchPipe } from './bill-search.pipe';

describe('BillSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BillSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
